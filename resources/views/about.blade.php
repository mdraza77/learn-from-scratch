<div>
    <!-- Breathing in, I calm body and mind. Breathing out, I smile. - Thich Nhat Hanh -->
    <h1>About Page</h1>
    <h1>{{ URL::current() }}</h1>
    <h1>{{ URL::full() }}</h1>
    <h1>{{ url()->current() }}</h1>
    <h1>{{ url()->full() }}</h1>
    <h1>{{ url()->previous() }}</h1>
    <a href="/home">About</a>
</div>
